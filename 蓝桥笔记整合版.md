# 蓝桥笔记整合版（按 C / C++ 划分）

> 更新：按"需不需要 STL"重新划分语言，整合速记页 + STL 工具 + 映射表 + 31 道题归类。
> 铁律：**同一道题内不混用 scanf/printf 和 cin/cout。**

## 划分标准（先记住）

| 情况 | 用什么 |
| --- | --- |
| 纯数组、循环、模拟、矩阵、计数、极值 | **C** |
| 用到 string / vector / set / sort / map / queue | **C++** |
| 核心算法模板（二分/前缀和/DFS/DP…） | 统一 **C++** |

---

# 第一部分：C 语言部分（基础套路速记页）

> 这些套路不需要 STL，用 C 写最快。每个记"心法 + 代码 + 易错点"。

## 套路 1：计数数组（抽屉法）

**心法**：用数值本身当数组下标，`cnt[x]++`

```c
int cnt[100005] = {0};          // 大小按数据范围开
for (int i = 0; i < n; i++) {
    int x; scanf("%d", &x);
    cnt[x]++;                   // 统计
}
// 去重：cnt[x] = 1（只标记出现过）
// 找第一个唯一：先统计，再按原顺序扫，找 cnt[x]==1
```

**易错**：负数下标加偏移 `cnt[x+offset]`；找"第一个只出现一次"要按原顺序扫；数据范围 10⁹ 时改用 map。

**对应题**：B2087、B2090、B2096、B2098、B2110

## 套路 2：极值维护（一次遍历动态更新）

**心法**：边读边比，只记"当前最优值 + 最优对象"

```c
int max_val = -1;               // 找最小用一个大数初值
char best[100];
for (int i = 0; i < n; i++) {
    int score; char name[100];
    scanf("%d %s", &score, name);
    if (score > max_val) {      // 找最小改成 <
        max_val = score;
        strcpy(best, name);
    }
}
printf("%s\n", best);
```

**易错**：找最大 `>`、找最小 `<`；"相等是否更新"决定用 `>` 还是 `>=`。

**对应题**：B2094、B2095、B2125、B2121

## 套路 3：矩阵坐标规律

**心法四行**（格子从 1 开始）：
- 同行：固定 r，列 1..n
- 同列：固定 c，行 1..n
- 主对角线（左上→右下）：`i - j` 恒定
- 副对角线（右上→左下）：`i + j` 恒定

```c
// 主对角线：j = i - (r - c)，判断 1<=j<=n
for (int i = 1; i <= n; i++) {
    int j = i - (r - c);
    if (j >= 1 && j <= n) { /* 输出 (i,j) */ }
}
// 副对角线：j = (r + c) - i，判断 1<=j<=n
```

**易错**：非方阵 n×m 时列范围改成 1..m。

**对应题**：B2099、B2100

## 套路 4：数组逆序 / 查找 / 连续段（小技巧三连）

```c
// ① 逆序输出（或双指针 swap）
for (int i = n-1; i >= 0; i--) printf("%d ", a[i]);

// ② 查找第一个满足条件的位置：单循环 + break
int pos = -1;
for (int i = 0; i < n; i++)
    if (a[i] == target) { pos = i; break; }

// ③ 最长连续相同段（最长平台）
int cur = 1, ans = 1;
for (int i = 1; i < n; i++)
    if (a[i] == a[i-1]) { cur++; if (cur > ans) ans = cur; }
    else cur = 1;
```

**易错**：逆序用双指针时 `l<r` 才 swap；找位置记得处理"找不到"。

**对应题**：B2089、B2091、B2093、B2097、B2092

---

# 第二部分：C++ 部分（STL 工具 + 字符串处理）

## STL 常用速查（背熟这 7 个）

```cpp
#include <bits/stdc++.h>
using namespace std;
// vector：push_back / size / 下标 / sort(v.begin(),v.end())
// string：size / += / substr / find / sort(s.begin(),s.end())
// stack：push / top / pop / empty
// queue：push / front / pop / empty
// priority_queue<int,vector<int>,greater<int>>：小顶堆
// map：mp[key]++ 计数，mp.count(key) 判断存在
// set：insert / count / erase（自动去重+排序）
```

## 字符串套路（用 string 比 char[] 方便）

### 套路 5：字符串扫描——遇分隔符结算

```cpp
string s;
getline(cin, s);              // 读整行（前面有 cin 时要先 cin.ignore()）
int i = 0, len = s.size();
while (i < len) {
    while (i < len && (s[i]==' '||s[i]==','||s[i]=='.')) i++;  // 跳分隔符
    if (i >= len) break;
    int start = i;
    while (i < len && s[i]!=' '&&s[i]!=','&&s[i]!='.') i++;    // 走到分隔
    // 结算：[start, i) 这段做你要的事（长度/翻转/最长）
}
```

**对应题**：B2120、B2121、B2122、B2156

### 套路 6：字符串逐字符变换

```cpp
string s; cin >> s;
for (char &c : s) {           // ★ 必须用引用 &
    if (isdigit(c)) { /* 统计数字 */ }
    c = toupper(c);           // 转大写 / tolower 转小写
    // 映射：if(c=='A')c='T'; else if(c=='T')c='A'; ...
    // 移位：c = (c - 'a' + k) % 26 + 'a';   // 小写循环移位 k
}
cout << s << "\n";
```

**易错**：遍历要 `char &c`；移位回绕取模 `%26`；映射别漏 else。

**对应题**：B2109、B2111、B2113、B2114、B2115、B2116、B2117

### 套路 7：子串 / 后缀 / 回文

```cpp
string s, t;
// 验证子串：s.find(t) != string::npos
// 删后缀：
int len = s.size();
if (len >= 3 && s.substr(len-3) == "ing") s = s.substr(0, len-3);
// 判断回文：双指针 或 reverse 后相等
string r = s; reverse(r.begin(), r.end()); if (r == s) { /* 回文 */ }
```

**对应题**：B2118、B2119、B2124、B2123

---

# 第三部分：蓝桥算法映射表

## A. 按数据范围

| 看到什么 | 想到什么 |
| --- | --- |
| n ≤ 10~20 | 搜索 / 状态压缩 |
| n ≤ 100~500 | O(n³)：Floyd / 区间DP |
| n ≤ 1000~5000 | O(n²)：朴素DP |
| n ≤ 10⁵ | 二分 / 排序 / 前缀和 / 双指针 / 树状数组 |
| n ≤ 10⁹ | 数学结论，不能遍历 |
| 数据大只问存在/计数 | 哈希 / map / set |

## B. 按题目问法

| 看到什么 | 想到什么 |
| --- | --- |
| 最大值最小 / 最小值最大 | 二分答案 |
| 多次问区间和 | 前缀和 |
| 多次区间统一加/减 | 差分数组 |
| 连续子数组 / 滑动窗口 | 双指针 |
| 统计次数 / 去重 / 找唯一 | 计数数组 / map / set |
| 连通块 / 分组 / 判环 | 并查集 / DFS / BFS |
| 多少种方案 / 最大收益 | DP |
| 每物品选一次 / 选无限次 | 0-1 背包 / 完全背包 |
| 非负权单源最短路 | Dijkstra |
| 依赖排序 / 判环 | 拓扑排序 |
| 大数次幂取模 | 快速幂 |
| 最大公约数 | gcd |

## C. 蓝桥特色信号

| 看到什么 | 想到什么 |
| --- | --- |
| 按分隔符拆词 | 分隔符结算扫描 |
| 日期 / 星期 / 天数差 | 闰年 + 每月天数 + 模拟 |
| 大数相加 | 高精度（字符串模拟） |
| 棋盘行列对角线 | 同行 r、同列 c、主对角 i−j、副对角 i+j |
| 结果填空题 | 写暴力程序算答案 |
| 题目长、像流水账 | 模拟（一步步实现） |

## D. 易混提醒

| 易混点 | 区分 |
| --- | --- |
| 二分开闭区间 | l/r 更新要配套 |
| 前缀和 vs 差分 | 前缀和=查询；差分=修改后统一查 |
| 0-1 vs 完全背包 | 只差循环方向（倒序/正序） |
| DFS vs BFS | DFS 标记+回溯；BFS 入队即标记 |

---

# 第四部分：31 道题归类总表

| 题号 | 题目 | 套路 | 语言 |
| --- | --- | --- | --- |
| B2087 | 与指定数字相同的数的个数 | 计数数组 | C |
| B2089 | 数组逆序重存放 | 逆序输出 | C |
| B2090 | 年龄与疾病 | 分区间计数 | C |
| B2091 | 向量点积计算 | 循环累加 | C |
| B2092 | 开关灯 | 模拟/约数 | C |
| B2093 | 查找特定的值 | 线性查找 | C |
| B2094 | 不与最大数相同的数字之和 | 极值+累加 | C |
| B2095 | 白细胞计数 | 极值+浮点（易错） | C |
| B2096 | 直方图 | 计数数组 | C |
| B2097 | 最长平台 | 连续段 | C |
| B2098 | 整数去重 | 标记去重 | C |
| B2099 | 矩阵交换行 | 矩阵行交换 | C |
| B2100 | 同行列对角线的格 | 矩阵坐标 | C |
| B2109 | 统计数字字符个数 | 逐字符判断 | C++ |
| B2110 | 找第一个只出现一次的字符 | 计数+string | C++ |
| B2111 | 基因相关性 | 逐位比较 | C++ |
| B2113 | 输出亲朋字符串 | 字符后移 | C++ |
| B2114 | 配对碱基链 | 字符映射 | C++ |
| B2115 | 密码翻译 | 移位回绕 | C++ |
| B2116 | 加密的病历单 | 综合变换（易错） | C++ |
| B2117 | 整理药名 | 大小写 | C++ |
| B2118 | 验证子串 | 子串查找 | C++ |
| B2119 | 删除单词后缀 | 后缀截断 | C++ |
| B2120 | 单词的长度 | 分词扫描 | C++ |
| B2121 | 最长最短单词 | 分词+极值 | C++ |
| B2122 | 单词翻转 | 分词+翻转 | C++ |
| B2123 | 字符串p型编码 | 连续段编码 | C++ |
| B2124 | 判断回文 | 回文 | C++ |
| B2125 | 最高分数的学生姓名 | 极值+对象 | C |
| B2156 | 最长单词2 | 分词 | C++ |
| B3652 | 渡荆门送别 | 综合 | C |

> 统计：C 语言记 15 道（数组/矩阵/模拟/计数/极值），C++ 记 16 道（字符串处理类）。

---

# 第五部分：代表题代码框架（C 版 vs C++ 版）

## 示例 1：B2096 直方图（计数，用 C）

```c
#include <stdio.h>
int cnt[100005] = {0};
int main() {
    int n, x; scanf("%d", &n);
    for (int i = 0; i < n; i++) { scanf("%d", &x); cnt[x]++; }
    for (int i = 0; i <= 100000; i++)
        if (cnt[i] > 0) printf("%d %d\n", i, cnt[i]);
    return 0;
}
```

## 示例 2：B2120 单词的长度（分词，用 C++）

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    ios::sync_with_stdio(false); cin.tie(nullptr);
    string s; getline(cin, s);
    int i = 0, len = s.size(), first = 1;
    while (i < len) {
        while (i < len && s[i]==' ') i++;
        if (i >= len) break;
        int start = i;
        while (i < len && s[i]!=' ') i++;
        if (!first) cout << ","; first = 0;
        cout << (i - start);
    }
    return 0;
}
```

## 示例 3：B2124 判断回文（C++）

```cpp
#include <bits/stdc++.h>
using namespace std;
int main() {
    string s; cin >> s;
    int l = 0, r = s.size() - 1, ok = 1;
    while (l < r) { if (s[l] != s[r]) { ok = 0; break; } l++; r--; }
    cout << (ok ? "yes" : "no") << "\n";
    return 0;
}
```

## 示例 4：B2098 整数去重（标记数组，用 C）

```c
#include <stdio.h>
int cnt[100005] = {0};
int main() {
    int n, x; scanf("%d", &n);
    for (int i = 0; i < n; i++) {
        scanf("%d", &x);
        if (!cnt[x]) { printf("%d ", x); cnt[x] = 1; }  // 第一次出现就输出并标记
    }
    return 0;
}
```

---

# 使用建议

1. **速记页 + STL 工具**：抄进活页本，每套路默写 3 次；
2. **映射表 A~D**：考前扫一眼，E 区随刷随加；
3. **31 题归类表**：用于查"这题属于哪个套路、该用什么语言"；
4. **代码框架**：4 个示例先看懂，再合上自己重写；
5. **易错题 2 道**（B2095、B2116）单独记进错题本。