# 数据结构（408）章节框架笔记

> 依据：王道《数据结构》单科书 + 408 统考大纲主干整理
> 用法：每学完一章，**合上书对着框架回忆**，想不起来/模糊的点就是你的薄弱点 → 标出来并补进《错题与背诵清单》。
> 说明：个别章节（串、并查集、红黑树等）在考纲中的优先级以王道当年标注为准；本笔记标 ★ 的是公认高频重点。

---

## 第1章 绪论

### 框架
- 数据结构三要素
  - 逻辑结构：线性结构 / 树形结构 / 图形结构 / 集合结构
  - 存储结构（物理结构）：顺序存储 / 链式存储 / 索引存储 / 散列存储
  - 数据的运算：运算的定义（针对逻辑结构）vs 运算的实现（依赖存储结构）
- 算法
  - 五个特性：有穷性、确定性、可行性、输入、输出
  - 好算法的标准：正确性、可读性、健壮性、高效率与低存储
- 复杂度
  - 时间复杂度：T(n) 的数量级 O(·)，只取最高阶、忽略系数
  - 空间复杂度：S(n)，含递归调用栈空间

### 核心考点
- 逻辑结构与存储结构的关系：同一逻辑结构可用不同存储结构实现
- 顺序存储 vs 链式存储特点对比（随机存取 vs 顺序存取；插入删除代价）
- 时间复杂度的计算：单层/多层循环、递归（写递推式）

### 易混·易错
- 平均/最坏/最好复杂度要看清题目问的是哪个（常问"最坏"）
- O(1) 表示常数阶，不代表只执行 1 次
- 存储密度：顺序存储 = 1，链式存储 < 1（有指针开销）

### 本章常考
- 选择题为主：逻辑/存储结构判断、复杂度计算。属于送分章，别丢分。

---

## 第2章 线性表 ★

### 框架
- 线性表定义：n 个同类型元素的有限序列；位序从 1 开始
- 顺序表（顺序存储）
  - 随机存取：LOC(ai)=LOC(a1)+(i-1)×sizeof
  - 插入/删除平均移动约 n/2 个元素
- 链表（链式存储）
  - 单链表：头结点作用；插入/删除需找前驱
  - 双链表：前驱后继都能 O(1) 找；插入删除改 4 个指针
  - 循环链表：单循环/双循环；判空条件不同
  - 静态链表：数组模拟指针，游标 cur
- 顺序表 vs 链表选型：查多/表长稳定→顺序；增删多/长度不定→链式

### 核心考点
- 头结点 vs 头指针的区别与好处（统一空表/非空表操作）
- 插入、删除、按值/按位查找的时间复杂度
- 双链表插入删除的指针操作顺序（先连后断）
- 常考算法题素材：逆置、合并两个有序表、找中间节点（快慢指针）、删除倒数第 k 个、就地删除

### 易混·易错
- "位序从 1 开始" vs 数组下标从 0 开始（写代码易错）
- 单链表删除节点必须知道前驱；不知道时用"值覆盖法"（把后继值拷过来删后继）
- 循环链表判空：头结点的 next 是否指向自身

### 本章常考
- 选择 + 大题算法题第一题常出自本章或树；顺序/链表的增删查与指针操作务必能手写。

---

## 第3章 栈、队列和数组

### 框架
- 栈（后进先出 LIFO）
  - 顺序栈：top 指针语义（指向栈顶元素 or 栈顶的下一个）
  - 链栈：头插即入栈
  - 共享栈：两端向中间生长
  - 应用：括号匹配、表达式求值（中缀转后缀）、递归、函数调用
- 队列（先进先出 FIFO）
  - 循环队列：队空/队满判定（牺牲一个单元 / 设 size / 设 tag）
  - 链式队列：队头出、队尾入
  - 双端队列：两端都可进出
  - 应用：层序遍历、缓冲区、广度优先
- 数组与特殊矩阵
  - 对称矩阵、三角矩阵、对角矩阵的压缩存储与下标映射
  - 稀疏矩阵：三元组表、十字链表

### 核心考点
- 循环队列：入队/出队指针变化、(rear+1)%MaxSize 取模、判空判满
- 栈在表达式求值中的作用：中缀 → 后缀（运算符优先级栈）
- 出栈序列合法性判断（选择题）
- 矩阵压缩存储下标计算（注意题目下标从 0 还是 1 开始）

### 易混·易错
- 循环队列判空/判满条件随实现方式不同（牺牲单元法：front==rear 空；(rear+1)%n==front 满）
- 数组下标是否从 0 开始，是压缩存储题丢分第一大坑
- 栈顶指针"先压后移"还是"先移后压"看清定义

### 本章常考
- 选择为主（出栈序列、循环队列、下标计算）；大题偶尔结合表达式/括号。

---

## 第4章 串

### 框架
- 串定义与存储：定长顺序串 / 堆分配 / 块链
- 模式匹配
  - 朴素匹配：O(m×n)
  - KMP：O(m+n)
    - next 数组：最长相等前后缀长度 + 1（失配时模式串右移）
    - nextval 优化：next 指向字符相同时继续前跳

### 核心考点
- next 数组的手算（低频但会考选择）
- 简单模式匹配的移动过程

### 易混·易错
- next 与 nextval 的区别与求法（nextval 消除 next 的冗余比较）
- 注意题目下标从 0 还是 1 开始，next 定义随之不同

### 本章常考
- 低频。统考近年直接考查较少，掌握 KMP 思想与 next 手算即可，不必深钻。

---

## 第5章 树与二叉树 ★

### 框架
- 基本概念：结点的度、树的度、层次、高度/深度、森林
- 二叉树
  - 性质：第 i 层至多 2^(i-1) 个；高 h 至多 2^h-1 个；n0 = n2 + 1
  - 完全二叉树：编号连续；结点 i 的孩子 2i、2i+1，父亲 ⌊i/2⌋
  - 存储：顺序存储（完全二叉树适用）/ 链式存储（二叉链表）
  - 遍历：先序 / 中序 / 后序 / 层序；由两种遍历序列唯一确定二叉树（须含中序）
- 线索二叉树：利用空链域指向前驱/后继（ltag/rtag）
- 树、森林与二叉树的转换：左孩子右兄弟
- 哈夫曼树与哈夫曼编码：WPL 最小；前缀编码
- 并查集（若考纲含）：集合合并与查找，路径压缩（按王道标注优先级）

### 核心考点
- 二叉树性质与结点数计算（选择题常考）
- 四种遍历的递归/非递归写法（非递归先序用栈、层序用队列）——代码题高频
- 由先/中/后序推二叉树
- 线索二叉树：如何找某结点的前驱/后继
- 哈夫曼编码构造与 WPL 计算（大题常考）
- 树 ↔ 二叉树 ↔ 森林转换

### 易混·易错
- "树的高度"从 1 还是 0 开始算（看题目定义）
- 完全二叉树 vs 满二叉树；度为 2 的树 vs 二叉树
- n0 = n2 + 1 只对二叉树成立（一般树不成立）
- 层序 = 队列；先序非递归 = 栈（入栈先右后左）

### 本章常考
- 高分重点章：选择 + 大题（遍历算法、哈夫曼、线索/树转换），算法题第二题常与树有关。

---

## 第6章 图 ★

### 框架
- 基本概念：有向/无向、度（有向分入度出度）、连通/强连通、生成树/生成森林、带权图
- 存储结构
  - 邻接矩阵：适合稠密图；空间 O(n²)
  - 邻接表：适合稀疏图；空间 O(n+e)
  - 十字链表（有向图）/ 邻接多重表（无向图）——了解
- 遍历
  - BFS（队列）：求无权图单源最短路
  - DFS（递归/栈）
  - 连通分量 / 生成树与遍历的关系
- 应用
  - 最小生成树：Prim（加点，O(V²)）/ Kruskal（加边+并查集，O(E log E)）
  - 最短路径：Dijkstra（单源，非负权）/ Floyd（各顶点间，允许负权无负环）
  - 拓扑排序（AOV）：有向无环图；判断有环
  - 关键路径（AOE）：事件最早/最迟、活动最早/最迟、关键活动

### 核心考点
- BFS/DFS 的遍历序列与邻接矩阵/邻接表下的结果
- Prim vs Kruskal 的适用与复杂度
- Dijkstra 逐步求解过程（大题）
- 拓扑排序序列、判断图是否有环
- 关键路径：求 ve/vl、e/l、关键活动（大题常考）

### 易混·易错
- 邻接表存储时遍历序列不唯一（与边表顺序有关）
- Dijkstra 不能处理负权；Floyd 不能有负环
- 拓扑排序"不唯一"；有环图无拓扑序列
- 关键路径可能不唯一；缩短关键活动不一定缩短工期（若有多条关键路径）

### 本章常考
- 高分重点章：选择 + 大题常考最短路径/关键路径/最小生成树步骤题。

---

## 第7章 查找

### 框架
- 线性结构查找
  - 顺序查找：ASL=(n+1)/2；对有序表可优化
  - 折半查找：仅适用于有序顺序表（随机存取）；判定树为平衡二叉树
  - 分块查找：块内无序、块间有序；索引表 + 顺序/折半
- 树形查找
  - 二叉排序树 BST：中序有序；插入/删除（删双分支用前驱或后继顶替）
  - 平衡二叉树 AVL：LL/RR/LR/RL 四种旋转；平衡因子 ±1
  - B 树 / B+ 树：多路平衡查找树，用于文件系统/数据库索引
- 散列查找
  - 散列函数构造：除留余数法等
  - 冲突处理：开放定址法（线性/平方探测、再散列）、链地址法
  - 装填因子 α = 表中记录数 / 表长；ASL 与 α 有关
- 平均查找长度 ASL：查找成功 / 查找失败

### 核心考点
- 折半查找的判定树与比较次数（⌊log2 n⌋+1）
- BST 的构造、删除、查找长度；AVL 四种旋转的手动调整
- B 树插入/删除的分裂与合并（了解过程）
- 散列表构造 + 各冲突处理下的 ASL 计算（成功/失败）——选择+大题高频
- ASL 公式记忆

### 易混·易错
- 折半查找要求"顺序存储"，链表不行
- 平衡调整：先找最小不平衡子树，再判断旋转类型
- 散列"查找失败"的 ASL 计算：按探测序列统计到空位为止
- ASL 成功与失败的分母不同（成功=记录数，失败=散列地址数/表长相关）

### 本章常考
- 选择 + 大题（散列表 ASL 计算、BST/AVL 构造）高频。

---

## 第8章 排序 ★

### 框架
- 插入类：直接插入（稳定）、折半插入、希尔排序（不稳定，增量序列）
- 交换类：冒泡（稳定）、快速排序（不稳定，基准元素，平均 O(n log n)）
- 选择类：简单选择（不稳定）、堆排序（不稳定，建堆+调整，O(n log n)）
- 归并类：二路归并（稳定，O(n log n)），空间 O(n)
- 基数排序：按位分配收集，稳定；适合位数少的关键字
- 外部排序：多路归并、败者树、置换-选择排序（了解）

### 核心考点
- 各排序的稳定性、时间/空间复杂度、适用场景（对比表）
- 一趟排序后的序列状态判断（快排、堆排、归并）
- 快排的划分过程（大题：手写一次 partition）
- 堆的插入/删除调整过程
- 希尔排序增量与趟数
- 排序算法选择：n 大且要求稳定→归并；n 大且不稳要求→快排/堆排

### 易混·易错
- 稳定性口诀：快选堆希 不稳定（快排、简单选择、堆排、希尔），其余常见算法稳定
- 堆排序"一次调整"vs"建堆"要分清
- 快排最坏 O(n²)（基本有序时退化），平均 O(n log n)
- 归并空间 O(n) 常被误记为 O(1)

### 本章常考
- 高分重点章：选择必考 + 大题常考（快排划分、堆调整、归并过程、排序算法对比）。