# 蓝桥杯代码模板本（C/C++ 大学B组）

> 目标：2027 年 4 月省赛 · 冲击省一
> 用法：这个本子的**唯一目的**是练到"闭卷 5 分钟内无错默写"。蓝桥杯是闭卷机考、不能带资料进场（以当年规则为准），所以模板不进考场，只用来平时练手。
> 配套：《蓝桥杯知识点备考清单.md》——清单打勾的标准就是"对应模板能默写 + 独立 AC 同类题"。

## 使用说明（每个模板怎么练）

1. **看**：先看"使用场景 + 易错点 + 代码骨架"，理解每一步在干什么。
2. **默写**：合上文件（或盖住代码区），在编译器 / 草稿纸上完整写出来，**不看任何参考**。
3. **核对**：写完打开骨架，或对照王道书 / 洛谷题解 / 蓝桥云课题解，逐行核对。
4. **记错**：写错的地方，补进该模板的"易错点"栏（那是你自己的坑）。
5. **达标**：同一个模板**连续 3 次默写无错**（不必隔天连续，但每次间隔 ≥1 天），才算会，才在总清单打勾。

> 易错点栏里预填的是常见坑，**请把自己每次默写/做题真实犯的错追加进去**——那才是对你最值钱的内容。

---

## 环境准备｜本地编译器开 C++11（换电脑 / 重装后先检查这一步）

> 本地 Dev-C++ 自带 **TDM-GCC 4.9.2**（2014 年的版本），**默认按 C++98 编译**，一遇到 `nullptr` 就直接报错：
> `error: 'nullptr' was not declared in this scope`

**开启方法（Dev-C++ 5.11）**：
1. `工具` → `编译选项`
2. 切到 `设置` → `代码生成`（Code Generation）
3. 找到 `语言标准 (-std)`，选 **ISO C++11 或 GNU C++11 都行**
   （推荐 **GNU C++11**：洛谷 / 蓝桥杯评测机都是 GCC 系，默认走 `gnu++` 模式，跟它一致最省心）
4. 确定，然后重新编译

> 手动在编译参数里加 `-std=c++11` 效果一样。
> 判断技巧：**本地编不过、洛谷能过** —— 先怀疑是不是这一步没开。

---

## 模板总清单（练熟一个勾一个）

**L0 基本功**
- [ ] T01 万能头 + 输入输出框架
- [ ] T02 sort + 结构体自定义排序
- [ ] T03 STL 容器速查（vector/stack/queue/priority_queue/map/set/string）
- [ ] T21 vector 读入与遍历（动态数组，最常用）
- [ ] T22 空格分隔输出（行尾不留空格）
- [ ] T23 变量命名避让清单（别和 STL 撞名）
- [ ] T24 循环边界：`n` 还是 `mx+1`
- [ ] T25 矩阵边界判断（B2101 边缘元素和）
- [ ] T26 行最大 / 列最小预处理（B2102 鞍点）
- [ ] T27 矩阵元素级运算：边读边处理（B2104 矩阵加法）
- [ ] T28 矩阵转置输出（B2106 矩阵转置）
- [ ] T31 矩阵乘法（B2105 矩阵乘法）

**L1 高频核心算法**
- [ ] T04 整数二分 / 二分答案
- [ ] T05 前缀和（一维 / 二维）
- [ ] T06 差分数组（区间加）
- [ ] T07 双指针 / 滑动窗口
- [ ] T08 DFS 模板（递归 + 回溯）
- [ ] T09 BFS 模板（队列）
- [ ] T10 线性 DP（数字三角形 / LIS 任选）
- [ ] T11 0-1 背包
- [ ] T12 完全背包
- [ ] T13 并查集（路径压缩）
- [ ] T14 gcd/lcm + 快速幂
- [ ] T15 质数筛（埃氏筛）
- [ ] T16 Dijkstra（堆优化）
- [ ] T17 拓扑排序（Kahn）

**蓝桥特色小工具**
- [ ] T18 日期与闰年
- [ ] T19 进制转换
- [ ] T20 高精度加法（可选）

---

## T01｜万能头 + 输入输出框架

- **使用场景**：每一道题。先把这个框架默写出来，再往里面填逻辑。
- **易错点**：`ios::sync_with_stdio(false)` 之后**不要混用 scanf/printf**；多组输入用 `while (cin >> n)`；结果可能很大就用 `long long`；**单引号 vs 双引号**——输出 `-1` 必须写 `cout << "-1"`，写成 `cout << '-1'` 会变成"多字符常量"，实际输出 `11569` 这种鬼数（编译器还会给 `-Wmultichar` 警告）。
- **代码骨架**（在此区默写完整代码）：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 读入数据
    // 处理逻辑
    // 输出结果
    return 0;
}
```

- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T02｜sort + 结构体自定义排序

- **使用场景**：给数组 / 结构体数组按某种规则排序。蓝桥排序题几乎必用。
- **易错点**：自定义比较函数写错返回逻辑（升序 `a < b`）；比较函数要写成"严格弱序"；对 `long long` 排序别用错类型。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

struct Node {
    int score;
    string name;
};

bool cmp(const Node &a, const Node &b) {
    // 在这里写：分数降序，同分按名字字典序
}

int main() {
    int n;
    cin >> n;
    vector<Node> v(n);
    // 读入
    sort(v.begin(), v.end(), cmp);
    // 输出
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T03｜STL 容器速查

- **使用场景**：栈（括号匹配）、队列（BFS）、优先队列（堆）、map/set（去重统计）、string（字符串处理）。
- **易错点**：`priority_queue` 默认大顶堆，小顶堆要 `greater<int>`；`stack/queue` 没有 `clear()`，要重新定义；`map` 用 `count()` 判断是否存在。
- **代码骨架**（把每个容器的"建、插、取、判空、删"默写一遍）：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    // stack<int> st;  st.push(x); st.top(); st.pop(); st.empty();
    // queue<int> q;   q.push(x);  q.front(); q.pop();  q.empty();
    // priority_queue<int, vector<int>, greater<int>> pq;  // 小顶堆
    // map<string, int> mp;   mp[key]++;   mp.count(key);
    // set<int> s;            s.insert(x); s.count(x);
    // string str;            str.substr(pos, len);  str.find(c);
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T04｜整数二分 / 二分答案

- **使用场景**：有序数组中查找；"最大值最小 / 最小值最大"类问题（二分答案）。
- **易错点**：**区间开闭不一致**是最大坑——`l = mid + 1` / `r = mid - 1` 要配套；`mid = l + (r - l) / 2` 防溢出；想清楚"找左边界还是右边界"。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

// 例：在升序数组 a 中找第一个 >= x 的位置（下标）
int lower_bound_(vector<int> &a, int x) {
    int l = 0, r = (int)a.size(); // 左闭右开
    while (l < r) {
        int mid = l + (r - l) / 2;
        if (a[mid] >= x) r = mid;
        else l = mid + 1;
    }
    return l;
}

int main() {
    // 在这里写：读入、调用、验证边界
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T05｜前缀和（一维 / 二维）

- **使用场景**：静态数组频繁求区间和；二维矩阵求子矩阵和。看到"多次询问一段区间的和"直接用。
- **易错点**：`sum[i] = sum[i-1] + a[i]` 下标从 1 开始存，避免 `i-1` 越界；二维前缀和公式 `s[i][j]=s[i-1][j]+s[i][j-1]-s[i-1][j-1]+a[i][j]` 记牢。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, q;
    cin >> n;
    vector<long long> a(n + 1), sum(n + 1, 0);
    for (int i = 1; i <= n; i++) cin >> a[i];
    for (int i = 1; i <= n; i++) {
        // 在这里写：sum[i] = ...
    }
    // 读入 q 次询问 [l, r]，输出 sum[r] - sum[l-1]
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T06｜差分数组（区间加）

- **使用场景**：多次给区间 `[l, r]` 统一加 v，最后求整个数组。看到"很多次区间操作 + 最后才查询"用差分。
- **易错点**：`d[l] += v; d[r+1] -= v;` 中 **r+1 可能越界，数组要开 n+2**；最后要前缀和还原。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, m;
    cin >> n >> m;
    vector<long long> a(n + 2, 0), d(n + 2, 0);
    for (int i = 1; i <= m; i++) {
        int l, r, v;
        cin >> l >> r >> v;
        // 在这里写：d[l] += v; d[r+1] -= v;
    }
    // 在这里写：对 d 做前缀和还原到 a，并输出
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T07｜双指针 / 滑动窗口

- **使用场景**：有序数组两数之和、连续子数组满足某条件的最短/最长区间。
- **易错点**：右指针扩展、左指针收缩的**时机**要想清楚；窗口内维护的"状态"（和/计数）每次移动只改一处。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    vector<int> a(n);
    // 读入

    int l = 0, ans = 0;
    long long cur = 0;
    for (int r = 0; r < n; r++) {
        cur += a[r];               // 右指针扩展
        while (cur > k) {          // 不满足条件时
            cur -= a[l];           // 左指针收缩
            l++;
        }
        ans = max(ans, r - l + 1); // 更新答案
    }
    cout << ans << endl;
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T08｜DFS 模板（递归 + 回溯）

- **使用场景**：全排列、组合、走迷宫找所有路径、枚举所有状态。
- **易错点**：进入递归前标记、回溯时**撤销标记**；递归出口写清楚；注意参数传递（值传递 vs 引用）。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int n;
bool used[15];

void dfs(int step) {
    if (step == n) {          // 出口
        // 在这里写：输出当前方案
        return;
    }
    for (int i = 1; i <= n; i++) {
        if (!used[i]) {
            used[i] = true;   // 标记
            // 在这里写：记录选择 / dfs(step + 1)
            used[i] = false;  // 回溯撤销
        }
    }
}

int main() {
    cin >> n;
    dfs(0);
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T09｜BFS 模板（队列）

- **使用场景**：迷宫最短步数、连通块扩散、逐层扩展类问题。
- **易错点**：入队时就要标记（否则会重复入队）；方向数组写对（上下左右）；坐标越界判断放最前。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int n, m;
char mp[105][105];
bool vis[105][105];
int dx[4] = {1, -1, 0, 0};
int dy[4] = {0, 0, 1, -1};

struct P { int x, y; };

int bfs(int sx, int sy) {
    queue<P> q;
    q.push({sx, sy});
    vis[sx][sy] = true;
    int step = 0;
    while (!q.empty()) {
        int sz = q.size();           // 按层扩展（若要统计步数）
        for (int i = 0; i < sz; i++) {
            P cur = q.front(); q.pop();
            for (int k = 0; k < 4; k++) {
                int nx = cur.x + dx[k], ny = cur.y + dy[k];
                if (nx < 0 || nx >= n || ny < 0 || ny >= m) continue; // 越界
                if (vis[nx][ny] || mp[nx][ny] == '#') continue;        // 已访问/障碍
                vis[nx][ny] = true;    // 入队即标记
                q.push({nx, ny});
            }
        }
        step++;
    }
    return step;
}

int main() {
    // 在这里写：读入地图，调用 bfs，输出结果
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T10｜线性 DP（数字三角形 / LIS）

- **使用场景**："从起点到终点取最大值"、"最长上升子序列"类递推题。
- **易错点**：DP 数组初始化和边界（`dp[0]` / 第一行第一列）；转移顺序要保证用到的状态已算好；LIS 朴素 O(n²) 会写即可，别贪 O(n log n)。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    // 数字三角形：
    // dp[i][j] = max(dp[i-1][j-1], dp[i-1][j]) + a[i][j];
    // 答案 = max(dp[n][j])

    // LIS：
    // for i in 1..n: for j < i: if (a[j] < a[i]) dp[i] = max(dp[i], dp[j] + 1);

    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T11｜0-1 背包

- **使用场景**：n 个物品选若干装入容量 V 的背包，每个物品最多选一次，求最大价值。
- **易错点**：**一维倒序**更新 `dp[j] = max(dp[j], dp[j-w[i]] + v[i])`（j 从大到小，保证每件只用一次）；`dp` 初始为 0（恰好装满则初始 -inf）。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, V;
    cin >> n >> V;
    vector<int> w(n + 1), v(n + 1);
    vector<int> dp(V + 1, 0);
    for (int i = 1; i <= n; i++) cin >> w[i] >> v[i];

    for (int i = 1; i <= n; i++) {
        for (int j = V; j >= w[i]; j--) {   // 倒序！
            dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
        }
    }
    cout << dp[V] << endl;
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T12｜完全背包

- **使用场景**：同 0-1 背包，但每个物品**可以取无限次**。
- **易错点**：一维**正序**更新（与 0-1 背包唯一区别）；把两个模板放一起对比记忆，别记反。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, V;
    cin >> n >> V;
    vector<int> w(n + 1), v(n + 1);
    vector<int> dp(V + 1, 0);
    for (int i = 1; i <= n; i++) cin >> w[i] >> v[i];

    for (int i = 1; i <= n; i++) {
        for (int j = w[i]; j <= V; j++) {   // 正序！
            dp[j] = max(dp[j], dp[j - w[i]] + v[i]);
        }
    }
    cout << dp[V] << endl;
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T13｜并查集（路径压缩）

- **使用场景**：判断连通性、合并集合、朋友圈/团伙分组类问题。
- **易错点**：`find` 用路径压缩（递归或迭代）；`merge` 前先 find 再判断是否同根；**下标从 1 到 n**。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int fa[100005];

int find(int x) {
    // 在这里写：路径压缩查找
    return fa[x] == x ? x : fa[x] = find(fa[x]);
}

void merge(int a, int b) {
    int ra = find(a), rb = find(b);
    if (ra != rb) fa[ra] = rb;   // 可加按秩合并优化
}

int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 1; i <= n; i++) fa[i] = i;
    // 在这里写：读入 m 条合并/询问，调用 find / merge
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T14｜gcd/lcm + 快速幂

- **使用场景**：约分、模意义下大数次幂计算（结果取模）。
- **易错点**：快速幂每一步都要取模；`lcm = a / gcd(a,b) * b` 先除再乘防溢出；指数用 long long。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

long long gcd(long long a, long long b) {
    return b == 0 ? a : gcd(b, a % b);
}

long long qpow(long long a, long long b, long long mod) {
    long long res = 1;
    while (b > 0) {
        if (b & 1) res = res * a % mod;
        a = a * a % mod;
        b >>= 1;
    }
    return res;
}

int main() {
    // 在这里写：调用验证 gcd / lcm / qpow
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T15｜质数筛（埃氏筛）

- **使用场景**：判断/统计 n 以内质数、质因数相关题。n ≤ 10^6 用埃氏筛足够。
- **易错点**：`isPrime[0]=isPrime[1]=false`；外层从 2 开始，内层从 i*i 开始筛（小优化）；开 `vector<bool>` 或 `bool[]`。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    int n;
    cin >> n;
    vector<bool> isPrime(n + 1, true);
    isPrime[0] = isPrime[1] = false;
    for (int i = 2; i * i <= n; i++) {
        if (isPrime[i]) {
            for (int j = i * i; j <= n; j += i) {
                isPrime[j] = false;    // 在这里写：标记合数
            }
        }
    }
    // 输出 n 以内质数个数
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T16｜Dijkstra（堆优化）

- **使用场景**：非负权单源最短路（数据量大时 O((n+e) log n)）。
- **易错点**：**Dijkstra 不能处理负权边**；`dist` 初始化 INF；优先队列存 `pair<距离, 节点>`，默认小顶堆要 `greater`；出队后检查 `d != dist[u]` 跳过旧记录。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int INF = 0x3f3f3f3f;
vector<pair<int, int>> g[100005];   // g[u] = {v, w}
int dist[100005];

void dijkstra(int s) {
    memset(dist, 0x3f, sizeof(dist));
    dist[s] = 0;
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<>> pq;
    pq.push({0, s});
    while (!pq.empty()) {
        auto [d, u] = pq.top(); pq.pop();
        if (d != dist[u]) continue;      // 跳过过期记录
        for (auto [v, w] : g[u]) {
            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pq.push({dist[v], v});
            }
        }
    }
}

int main() {
    // 在这里写：读入 n, m, s 和边，调用 dijkstra(s)，输出 dist
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T17｜拓扑排序（Kahn）

- **使用场景**：有向无环图的先后依赖排序；判断是否有环。
- **易错点**：用**入度数组 + 队列**（入度为 0 的先入队）；结果长度 < n 说明有环；输出顺序不唯一。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

vector<int> g[100005];
int indeg[100005];

int main() {
    int n, m;
    cin >> n >> m;
    for (int i = 0; i < m; i++) {
        int u, v;
        cin >> u >> v;
        g[u].push_back(v);
        indeg[v]++;
    }
    queue<int> q;
    vector<int> ans;
    for (int i = 1; i <= n; i++) if (indeg[i] == 0) q.push(i);
    while (!q.empty()) {
        int u = q.front(); q.pop();
        ans.push_back(u);
        for (int v : g[u]) {
            if (--indeg[v] == 0) q.push(v);   // 减入度，变 0 入队
        }
    }
    if (ans.size() < n) cout << "有环" << endl;
    else for (int x : ans) cout << x << " ";
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T18｜日期与闰年

- **使用场景**：蓝桥常考日期题（星期几、相隔天数、日期合法性）。
- **易错点**：闰年判断 `(y%4==0 && y%100!=0) || y%400==0`；每月天数数组记牢；跨年循环模拟时先判闰年再定二月。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int daysInMonth[13] = {0,31,28,31,30,31,30,31,31,30,31,30,31};

bool isLeap(int y) {
    // 在这里写：闰年判断
}

int daysOfMonth(int y, int m) {
    if (m == 2 && isLeap(y)) return 29;
    return daysInMonth[m];
}

int main() {
    // 在这里写：读入日期，用 daysOfMonth 循环累加 / 判断合法
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T19｜进制转换

- **使用场景**：十进制 ↔ 其他进制互转、数位拼接题。
- **易错点**：十进制转其他进制要**倒序**输出（先存栈/数组）；其他进制转十进制用累乘；超过 10 用字母 A-F 表示。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

string toBase(int x, int base) {
    string s = "";
    // 在这里写：短除法，x % base 得最低位，x /= base，最后反转
    return s;
}

int main() {
    // 在这里写：读入，调用 toBase，输出
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T20｜高精度加法（可选）

- **使用场景**：数字超过 long long 范围的大整数加法。蓝桥低频，会加法即可。
- **易错点**：字符串低位对齐（反转）；进位变量 carry 最后可能还有 1；输出别忘反转。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

string addBig(string a, string b) {
    reverse(a.begin(), a.end());
    reverse(b.begin(), b.end());
    // 在这里写：逐位相加 + 进位，结果存 string，最后反转返回
    return "";
}

int main() {
    string a, b;
    cin >> a >> b;
    cout << addBig(a, b) << endl;
    return 0;
}
```

- **达标勾选**：□ 第1次（____）　□ 第2次（____）　□ 第3次（____）

---

## T21｜vector 读入与遍历（最常用的动态数组）

- **使用场景**：数据个数事先不确定、或想按 n 动态开数组时，首选 vector。几乎每道题都会用到。
- **为什么用它**：比 `int a[105]` 安全——数组大小按 n 动态开，不会因为"开小了"而越界。
- **易错点**：
  - `vector<int> a(n);` 是"开 n 个元素"，下标 `0 ~ n-1`，别越界
  - `#include <bits/stdc++.h>` 已包含 `<vector>`，不用再单独 include
  - 别搞混：vector 用 `a.begin() / a.end()`，C 数组用 `a, a+n`
  - 变量别命名成 `count`（会挡住 `std::count`）
  - **【本人踩坑 · 2026-09-10 · B2093】** 读入循环写成了 `for (int i = 1; i < n; i++)`，把 `a[0]` 漏读了，结果全错。**读入循环的起点一定是 `i = 0`**——C++ 下标从 0 开始，`i = 1` 会跳过第 0 个元素。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n;
    cin >> n;
    vector<int> a(n);                     // 按 n 动态开
    for (int i = 0; i < n; i++) cin >> a[i];

    for (int i = 0; i < n; i++) cout << a[i] << ' ';   // 下标遍历
    for (int x : a) cout << x << ' ';                  // 只读遍历
    for (int &x : a) x++;                              // 可改遍历

    return 0;
}
```

- **常用操作速记**：
  - `vector<int> v;` 建空容器　`vector<int> v(n);` 开 n 个
  - `v.push_back(x);` 尾部加　`v.pop_back();` 尾部删
  - `v.size()` 元素个数　`v.clear()` 清空
  - `v.begin(), v.end()` 配合 `sort / count / reverse`
- **配套练习**：洛谷 B2087（与指定数字相同的数的个数），用 vector 重写一遍
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T22｜空格分隔输出（别留行尾空格）

- **使用场景**：题目要求"输出一行，每两个整数之间用空格分隔"。几乎每道输出数组的题都要用（如 B2089 数组逆序重存放）。
- **易错点**：
  - 直接写 `cout << a[i] << ' ';` 会**多出一个行尾空格**。洛谷通常忽略它，但**部分 OJ 严格比对，会直接判 WA**，蓝桥杯也建议别留。
  - 最后一个数字后面不能有空格；末尾的换行 `'\n'` 也别忘。
  - 判断"是不是第一个"用 `if (i)`（等于 `i != 0`），比 `i == 0 ? "" : " "` 之类更简洁。
- **代码骨架**（推荐，n 为 0 也安全）：

```cpp
for (int i = 0; i < n; i++) {
    if (i) cout << ' ';      // 不是第一个，才先打空格
    cout << a[i];
}
cout << '\n';
```

- **另一种写法**（只在 n >= 1 时安全，n 可能为 0 就别用）：

```cpp
cout << a[0];
for (int i = 1; i < n; i++) cout << ' ' << a[i];
cout << '\n';
```

- **配套练习**：洛谷 B2089（数组逆序重存放）。原来的写法 `cout << a[i] << ' ';` 会多一个行尾空格，用上面第一种重写一遍。
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T23｜变量命名避让清单（别和 STL 撞名）

- **使用场景**：每次起变量名之前扫一眼。写了 `using namespace std;` 之后，标准库的名字在全局都可见，拿它们当变量名会把原本的函数**遮蔽**掉。
- **为什么危险**：**编译器不报错**（遮蔽是合法的），你可能几十行内都发现不了——直到某天你调 `max(a, b)`，它才炸：

```text
error: 'max' cannot be used as a function
```

- **本人踩坑记录**：
  - 2026-09-10 · B2087：变量起名 `count`，遮住了 `std::count`
  - 2026-09-11 · B2094：变量起名 `max`，遮住了 `std::max`
- **别用这些名字当变量**（`using namespace std;` 之后它们都是标准库的）：

```text
count  max   min   sort  swap  find  reverse  distance
data   time  left  right end   next  prev   remove
fill   equal move  copy  begin size  abs    pow   log
```

- **推荐替代写法**：

| 想表达 | 别写 | 改写成 |
| --- | --- | --- |
| 计数 | `count` | `cnt` |
| 最大值 | `max` | `mx` |
| 最小值 | `min` | `mn` |
| 距离 | `distance` | `dis` |
| 时间 | `time` | `tm` |
| 左右指针 | `left` / `right` | `l` / `r` |
| 前后 | `next` / `prev` | `nxt` / `pre` |
| 长度大小 | `size` | `sz` |
| 绝对值 | `abs` | `val` |

- **速记口诀**：**能一眼看出"这是变量"的名字才安全。** `mx`、`cnt`、`sz` 这类缩写永远不会和标准库撞。
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T24｜循环边界：`n` 还是 `mx+1`

- **使用场景**：写任何循环前，先问自己一句——我在遍历**数据**，还是在遍历**值域**？
- **两个量完全不同**：

| 量 | 含义 | 用在哪种循环 |
| --- | --- | --- |
| `n` | 有**多少个数** | 遍历数据：读入、扫描、计数 |
| `mx+1` | 数值**范围**多大（0 到 mx） | 遍历值域：按值输出频数 |

- **本人踩坑记录**：2026-09-11 · B2096 直方图 —— 输出频数时把 `i < mx+1` 写成了 `i < n`。
- **两个失败方向（都实测过）**：

| 情况 | 结果 |
| --- | --- |
| `n` 比 `mx+1` 大 | **越界读** → 输出垃圾值（实测多印了一行 `-1`） |
| `n` 比 `mx+1` 小 | **少输出** → 答案不完整（实测该印 6 行、只印了 1 行） |
| `n` 正好等于 `mx+1` | **碰巧全对** ← 最危险，你根本发现不了 |

- **为什么危险**：**编译器不报错。** 尤其第三种情况，代码跑得好好的，换个测试数据才炸。
- **口诀**：

```text
遍历“有几个数”   →  n
遍历“值能取到几” →  mx + 1
```

- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T25｜矩阵边界判断

- **使用场景**：求矩阵边缘元素之和、输出外圈、处理第一行/最后一行/第一列/最后一列。典型题：洛谷 B2101 计算矩阵边缘元素之和。
- **核心转换**：不要分别把“四条边”加起来，而是遍历每个格子，判断它是否在边缘：

```text
第一行   → i == 0
最后一行 → i == m - 1
第一列   → j == 0
最后一列 → j == n - 1
```

- **易错点**：
  - 如果把四条边分别相加，**四个角会被重复计算**；遍历每个格子再判断，角只算一次。
  - `m == 1` 或 `n == 1` 时，条件仍然成立，所有元素都属于边缘。
  - 求和用 `long long sum`，不要用 `int`；变量名别叫 `cnt`，`cnt` 通常表示“个数”。
- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int m, n;
    cin >> m >> n;

    long long sum = 0;

    for (int i = 0; i < m; i++) {
        for (int j = 0; j < n; j++) {
            int x;
            cin >> x;

            if (i == 0 || i == m - 1 ||
                j == 0 || j == n - 1) {
                sum += x;
            }
        }
    }

    cout << sum << '\n';
    return 0;
}
```

- **变式提醒**：如果后面还要用到矩阵，可以先把矩阵存进 `vector<vector<int>>`，再遍历判断；如果只用求和，边读边加更省空间。
- **配套练习**：洛谷 B2101。
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T26｜行最大 / 列最小预处理（B2102 鞍点）

- **使用场景**：判断一个元素是否同时满足“所在行最大”和“所在列最小”；矩阵中寻找鞍点。典型题：洛谷 B2102 计算鞍点。
- **为什么先预处理**：不要对每个格子都重新扫描整行、整列。先一次性求出：

```text
rowMax[i] = 第 i 行的最大值
colMin[j] = 第 j 列的最小值
```

然后只需要判断：

```cpp
if (a[i][j] == rowMax[i] &&
    a[i][j] == colMin[j]) {
    // 找到鞍点
}
```

- **易错点**：
  - 求行最大值时用 `>`，只有发现更大的数才更新。
  - 求列最小值时用 `<`，只有发现更小的数才更新。
  - 最后的鞍点判断用 `==`，不要混用 `<=`、`>=`。
  - `rowMax` 和 `colMin` 要分别初始化成每行、每列的第一个元素。
- **代码骨架**（以 `5 × 5` 为例）：

```cpp
#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    const int N = 5;
    int a[N][N];
    int rowMax[N];
    int colMin[N];

    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            cin >> a[i][j];
        }
    }

    // 1. 求每行最大值
    for (int i = 0; i < N; i++) {
        rowMax[i] = a[i][0];
        for (int j = 1; j < N; j++) {
            if (a[i][j] > rowMax[i]) {
                rowMax[i] = a[i][j];
            }
        }
    }

    // 2. 求每列最小值
    for (int j = 0; j < N; j++) {
        colMin[j] = a[0][j];
        for (int i = 1; i < N; i++) {
            if (a[i][j] < colMin[j]) {
                colMin[j] = a[i][j];
            }
        }
    }

    // 3. 找同时满足条件的元素
    for (int i = 0; i < N; i++) {
        for (int j = 0; j < N; j++) {
            if (a[i][j] == rowMax[i] &&
                a[i][j] == colMin[j]) {
                cout << i + 1 << ' '
                     << j + 1 << ' '
                     << a[i][j] << '\n';
                return 0;
            }
        }
    }

    cout << "not found\n";
    return 0;
}
```

- **复杂度**：预处理 `O(N²)`，判断 `O(N²)`，总计 `O(N²)`；比每个格子都重扫整行整列更适合大矩阵。
- **配套练习**：洛谷 B2102；再自造一组“无鞍点”和“带并列值”的数据测试。
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T27｜矩阵元素级运算：边读边处理（B2104 矩阵加法）

- **使用场景**：两个同规格矩阵做逐元素运算，例如相加、相减。典型题：洛谷 B2104 矩阵加法。
- **核心思想**：如果第二个矩阵只需要参与运算、不需要保存，就不要额外开 `b[M][N]`。可以读完第一个矩阵后，边读第二个矩阵的数据边处理。
- **两种写法**：

```cpp
// 写法 1：边读 B，边累加到 A
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        int x;
        cin >> x;
        a[i][j] += x;
    }
}

for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        if (j) cout << ' ';
        cout << a[i][j];
    }
    cout << '\n';
}
```

```cpp
// 写法 2：边读 B，边输出 A + B，不修改 A
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        int x;
        cin >> x;

        if (j) cout << ' ';
        cout << a[i][j] + x;
    }
    cout << '\n';
}
```

- **易错点**：
  - 第二个矩阵的读取顺序要和输出顺序一致，按行、按列读入。
  - 行尾不要多输出空格，用 `if (j) cout << ' ';`。
  - `n` 和 `m` 分别代表行数、列数，循环边界不要写反。
  - 本题元素最大 `1000`，两数相加最大 `2000`，用 `int` 足够；但通用模板里如果数值可能很大，要用 `long long`。
- **和 B2103 对比**：

```text
B2103 图像相似度：需要比较 A 和 B，所以至少要保存 A，再读 B 比较。
B2104 矩阵加法：只需要相加，可以读完 A 后，边读 B 边累加或直接输出。
```

- **配套练习**：洛谷 B2104。
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T28｜矩阵转置输出（B2106 矩阵转置）

- **使用场景**：输入一个 `m` 行 `n` 列的矩阵，要求输出它的转置矩阵，即输出 `n` 行 `m` 列。典型题：洛谷 B2106 矩阵转置。
- **核心映射**：

```text
原矩阵：m 行 n 列
转置后：n 行 m 列

转置后的第 col 行、第 row 列
等于原矩阵的第 row 行、第 col 列
也就是 a[row][col]
```

- **最简写法（直接输出，不需要真正交换或另开数组）**：

```cpp
for (int col = 0; col < n; col++) {      // 原来的列，变成新的行
    for (int row = 0; row < m; row++) {  // 原来的行，变成新的列
        if (row) cout << ' ';
        cout << a[row][col];
    }
    cout << '\n';
}
```

- **如果先保存到转置矩阵 `b`**：

```cpp
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        b[j][i] = a[i][j];
    }
}

for (int i = 0; i < n; i++) {      // 转置后 n 行
    for (int j = 0; j < m; j++) {  // 每行 m 个
        if (j) cout << ' ';
        cout << b[i][j];
    }
    cout << '\n';
}
```

- **如果是方阵原地转置**：

```cpp
if (m == n) {
    for (int i = 0; i < m; i++) {
        for (int j = i + 1; j < n; j++) {
            swap(a[i][j], a[j][i]);
        }
    }
}
```

  方阵原地转置时 `j` 必须从 `i + 1` 开始。若从 `j = 0` 开始，每个元素会被交换两次，矩阵又变回原样。

- **易错点**：
  - 原矩阵是 `m` 行 `n` 列，转置后是 `n` 行 `m` 列；输出循环必须变成**外层 `n`、内层 `m`**。
  - 直接输出时写 `a[row][col]`，不要下意识写成 `a[col][row]`。
  - 使用辅助数组时，逻辑上应把 `b` 看成 `n` 行 `m` 列；即使为了省事统一开成 `MAXN × MAXN`，循环边界仍要按 `n × m` 写。
  - 行尾不要多输出空格，用 `if (row) cout << ' ';` 或 `if (j) cout << ' ';`。

- **复杂度**：
  - 直接输出：时间 `O(m × n)`，额外空间 `O(1)`。
  - 保存到 `b`：时间 `O(m × n)`，额外空间 `O(m × n)`。
  - 方阵原地交换：时间 `O(n²)`，额外空间 `O(1)`。

- **配套练习**：洛谷 B2106；再分别用方阵和非方阵各测一次。
- **达标勾选**：□ 第1次（日期：____）　□ 第2次（____）　□ 第3次（____）

---

## T29｜矩阵顺时针旋转90度（B2107 图像旋转）

- **使用场景**：输入一个 `m` 行 `n` 列的矩阵，将它顺时针旋转 `90°` 后输出。典型题：洛谷 B2107 图像旋转。
- **旋转前后的形状**：

```text
原矩阵：m 行 n 列
顺时针旋转90°后：n 行 m 列
```

- **核心映射**：

```text
原矩阵位置 (i, j)
顺时针旋转90°后，放到新矩阵位置 (j, m - 1 - i)

所以：
b[j][m - 1 - i] = a[i][j];
```

- **最简写法**：

```cpp
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        b[j][m - 1 - i] = a[i][j];
    }
}

for (int i = 0; i < n; i++) {      // 旋转后 n 行
    for (int j = 0; j < m; j++) {  // 每行 m 个
        if (j) cout << ' ';
        cout << b[i][j];
    }
    cout << '\n';
}
```

- **为什么是 `b[j][m - 1 - i]`**：
  - 原矩阵有 `m` 行，行下标是 `0 ~ m-1`。
  - 顺时针旋转后，原来的“行”会变成新矩阵的一列。
  - 原来的第 `i` 行，会跑到新矩阵从右往左数的第 `i+1` 列，所以列下标是 `m - 1 - i`。
  - 原来的第 `j` 列，会变成新矩阵的第 `j` 行，所以新行下标是 `j`。

- **和转置对比**：

```cpp
// 转置
b[j][i] = a[i][j];

// 顺时针旋转90°
b[j][m - 1 - i] = a[i][j];

// 逆时针旋转90°
b[n - 1 - j][i] = a[i][j];
```

- **易错点**：
  - 旋转后行列数互换：由 `m × n` 变成 `n × m`。
  - 公式里的 `m` 是原矩阵的行数，不要下意识写成 `n`。
  - 输出时外层是 `n`，内层是 `m`，不要沿用原来的循环边界。
  - 行尾不要多输出空格，使用 `if (j) cout << ' ';`。
  - 如果只有原矩阵 `a`，也可以直接按旋转后的顺序输出；但用辅助矩阵 `b` 更直观，适合刚开始练。

- **复杂度**：
  - 时间：`O(m × n)`
  - 额外空间：`O(m × n)`（保存旋转后的矩阵）

- **配套练习**：洛谷 B2107；做完后与 B2106 矩阵转置对比一次。
- **达标勾选**：□ 重新默写公式　□ 用 `2 × 3` 非方阵测试　□ 用 `3 × 3` 方阵测试　□ 检查输出行列数与行尾空格

---

## T30｜矩阵边缘保留 + 内部五格平均（B2108 图像模糊处理）

- **使用场景**：矩阵边缘像素保持不变；内部像素取“自己 + 上下左右”共 `5` 个数的平均值，并按四舍五入输出。典型题：洛谷 B2108 图像模糊处理。
- **核心规则**：

```text
边缘像素：原值不动
内部像素：自己、上、下、左、右，一共5个数求平均
平均值：四舍五入
```

- **正确骨架**：

```cpp
for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        cin >> a[i][j];
        b[i][j] = a[i][j];       // 先复制，边缘自然保持原值
    }
}

for (int i = 1; i < m - 1; i++) {
    for (int j = 1; j < n - 1; j++) {
        int sum = a[i][j]
                + a[i - 1][j]
                + a[i + 1][j]
                + a[i][j - 1]
                + a[i][j + 1];

        b[i][j] = (sum + 2) / 5; // +2 等价于除以5后的四舍五入
    }
}

for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        if (j) cout << ' ';
        cout << b[i][j];
    }
    cout << '\n';
}
```

- **为什么不能直接改 `a`**：
  - 当前像素要读取上下左右的原值。
  - 如果直接修改 `a`，后面计算其他像素时可能读到“已经被模糊过”的值，结果就错了。
  - 所以这里用 `a` 保存原图，用 `b` 保存新图。

- **四舍五入记忆**：

```text
sum / 5      直接截断，不四舍五入
(sum + 2) / 5  四舍五入
```

因为分母是 `5`，整数除法前加 `2`，相当于给小数部分达到或超过一半的情况进位。

- **边界循环**：

```text
i 从 1 开始，到 m - 2
j 从 1 开始，到 n - 2
```

正好避开最外层的边缘像素。

- **易错点**：
  - 只算了上下左右，没有把自己算进去。
  - 把 `5` 个数写成了 `4` 个数。
  - 直接写 `sum / 5`，忘记四舍五入。
  - 直接修改原矩阵 `a`，影响后面的邻居读取。
  - 边缘像素也参与了平均，导致边缘被错误修改。

- **我的真实错误（2026-09-17）**：
  - **错误1：没有理解“内部像素”的范围**。看 AI 思路之前，不知道要把“自己 + 上下左右”一起计算；正确做法是共 `5` 个数，不能只看四个邻居。
  - **错误2：四舍五入没有处理对**。不能直接写 `sum / 5`，因为整数除法会截断小数；正确写法是 `(sum + 2) / 5`。
  - **记忆句**：

```text
五格平均：自己 + 上下左右
四舍五入：sum 先加 2，再除以 5
旧图算数据，新图存答案
```

- **为什么除以 5 要加 2**：

| `sum % 5` 的余数 | 实际小数部分 | 是否进位 |
|---|---:|---|
| 0 | `.0` | 不进位 |
| 1 | `.2` | 不进位 |
| 2 | `.4` | 不进位 |
| 3 | `.6` | 进位 |
| 4 | `.8` | 进位 |

整数除法会直接丢掉小数。加上 `2` 后，余数为 `3` 或 `4` 的数会刚好超过下一个 `5` 的倍数，于是自动多进 `1`：

```text
11 / 5 = 2.2 → (11 + 2) / 5 = 13 / 5 = 2
12 / 5 = 2.4 → (12 + 2) / 5 = 14 / 5 = 2
13 / 5 = 2.6 → (13 + 2) / 5 = 15 / 5 = 3
14 / 5 = 2.8 → (14 + 2) / 5 = 16 / 5 = 3
```

它等价于：

```cpp
int ans = sum / 5;
if (sum % 5 >= 3) ans++;   // 余数是 3 或 4 才进位
```

- **以后遇到“四舍五入”怎么办**：
  - 如果要输出整数，且数值非负：除以 `d` 时写成 `(sum + d / 2) / d`。
  - 例如除以 `5`：`(sum + 2) / 5`；除以 `10`：`(sum + 5) / 10`。
  - 如果要保留小数，不要用整数除法，改用 `double`、`fixed` 和 `setprecision`。
  - 如果只是把浮点数变成最近整数，可以用 `round(x)`。
  - 如果数值接近 `int` 上限，先换成 `long long`，避免 `sum + d / 2` 溢出。

- **复杂度**：
  - 时间：`O(m × n)`
  - 额外空间：`O(m × n)`（保存模糊后的矩阵）

- **配套练习**：洛谷 B2108；做完后自己解释一次“为什么必须用 `b` 保存结果”。
- **达标勾选**：□ 重新默写五格平均公式　□ 测试边缘不变　□ 测试四舍五入　□ 测试 `m < 3` 或 `n < 3` 的退化情况

---

## T31｜矩阵乘法（B2105）

- **使用场景**：计算两个矩阵的乘积。典型题：洛谷 B2105 矩阵乘法。
- **维度关系**：先写形状，再写循环。

```text
A：n 行 × m 列
B：m 行 × k 列
C：n 行 × k 列

A 的列数 = B 的行数 = m
B 的列数 = C 的列数 = k
```

- **核心公式**：

```text
C[i][j] = A 的第 i 行 × B 的第 j 列
        = A[i][0] × B[0][j]
        + A[i][1] × B[1][j]
        + …
        + A[i][m-1] × B[m-1][j]
```

- **代码骨架**：

```cpp
#include <bits/stdc++.h>
using namespace std;

const int MAXN = 105;
int a[MAXN][MAXN];
int b[MAXN][MAXN];
long long c[MAXN][MAXN];

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n, m, k;
    cin >> n >> m >> k;

    // A：n 行 m 列
    for (int i = 0; i < n; i++)
        for (int j = 0; j < m; j++)
            cin >> a[i][j];

    // B：m 行 k 列
    for (int i = 0; i < m; i++)
        for (int j = 0; j < k; j++)
            cin >> b[i][j];

    // C：n 行 k 列
    for (int i = 0; i < n; i++) {
        for (int j = 0; j < k; j++) {
            for (int t = 0; t < m; t++) {
                c[i][j] += a[i][t] * b[t][j];
            }
        }
    }

    for (int i = 0; i < n; i++) {
        for (int j = 0; j < k; j++) {
            if (j) cout << ' ';
            cout << c[i][j];
        }
        cout << '\n';
    }

    return 0;
}
```

- **三个循环变量**：

```text
i：C 的行号，也是 A 的行号
j：C 的列号，也是 B 的列号
t：公共维度，范围是 0 ~ m-1
```

- **最里面一行的意思**：

```cpp
c[i][j] += a[i][t] * b[t][j];
```

`a[i][t]` 在 A 的第 `i` 行向右移动；  
`b[t][j]` 在 B 的第 `j` 列向下移动；  
两者对应相乘，并累加到 C 的 `(i, j)` 格子。

- **易错点**：
  - **先把 A、B、C 的形状写出来**，不要一上来就写循环。
  - B 是 `m` 行 `k` 列；读取 B 时外层是 `m`，内层是 `k`，不能写反。
  - 公式是 `a[i][t] * b[t][j]`，不要写成 `a[i][t] * b[j][t]`。
  - C 要初始为 0；全局数组会自动清零，局部数组要写 `= {}`。
  - 多组数据时，每组都要 `memset(c, 0, sizeof(c));`。
  - 乘积或总和可能很大时，用 `long long`，不要只用 `int`。

- **复杂度**：
  - 时间：`O(n × m × k)`
  - 空间：`O(n × m + m × k + n × k)`

- **我的真实错误（2026-09-17）**：
  - 刚开始做 B2105 时，把 `k` 和 `m` 的顺序弄反了。
  - 错因不是乘法公式本身，而是**没有先确认 B 的形状**：把 B 错当成 `k` 行 `m` 列，导致读取和后续下标对应关系混乱。
  - 防错口诀：

```text
A 的列 = B 的行 = m
B 的列 = C 的列 = k
```

- **下次先做三步**：
  1. 写下 `A(n×m) × B(m×k) = C(n×k)`；
  2. 检查 `A列 = B行`、`B列 = C列`；
  3. 再写读取、乘法、输出三层循环。

- **配套练习**：洛谷 B2105；做完后自己解释一次 `b[t][j]` 为什么下标是 `t` 在前、`j` 在后。
- **达标勾选**：□ 能写出矩阵形状　□ 能解释三重循环　□ 能独立默写核心公式　□ 自造非方阵数据测试

---

## 错题追记｜B2102 计算鞍点：严格不等号与等号边界

- **日期**：2026-09-14
- **题目条件**：给定一个 `5 × 5` 矩阵，每行只有一个最大值，每列只有一个最小值。若存在元素同时是所在行的最大值、所在列的最小值，就输出它的行、列和值；否则输出 `not found`。
- **我的错误**：计算鞍点时，把 `<` 写成了 `<=`，把 `>` 写成了 `>=`。
- **为什么不能这样写**：
  - 找行最大值时，`>` 表示"只有发现更大的数才更新"；写成 `>=`，遇到并列值时会换成后面的位置，容易漏掉真正的鞍点。
  - 检查列最小值时，只有发现"比候选值更小的数"才能否定候选；写成 `<=`，遇到相等的数也会错误否定。
  - 本题虽然保证每行、每列唯一，所以有些测试数据可能"碰巧不炸"；但边界习惯一错，换成允许并列的题目就会出错。
- **记忆口诀**：

```text
最大值：只有更大才更新   →  >
最小值：只有更小才否定   →  <
等号表示并列，不能随便当成不满足
```

- **正确代码骨架**：

```cpp
int a[5][5];
for (int i = 0; i < 5; i++)
    for (int j = 0; j < 5; j++)
        cin >> a[i][j];

for (int i = 0; i < 5; i++) {
    int mx = a[i][0], mxj = 0;

    // 找这一行的最大值及其列号
    for (int j = 1; j < 5; j++) {
        if (a[i][j] > mx) {
            mx = a[i][j];
            mxj = j;
        }
    }

    // 检查该值是否也是所在列的最小值
    bool ok = true;
    for (int k = 0; k < 5; k++) {
        if (a[k][mxj] < a[i][mxj]) {
            ok = false;
            break;
        }
    }

    if (ok) {
        cout << i + 1 << ' ' << mxj + 1 << ' ' << a[i][mxj] << '\n';
        return 0;
    }
}

cout << "not found\n";
```

- **本题的注意点**：题目虽然保证"每行只有一个最大值、每列只有一个最小值"，但这只是题目给出的特殊条件；写代码时仍然应该用正确的严格比较，不能靠题目数据碰巧不出现并列。
- **达标勾选**：□ 重新默写一次　□ 换一组无鞍点数据测试　□ 用带并列值的自造数据测试

---

## 错题追记｜B2103 图像相似度：整数除法与分母括号

- **日期**：2026-09-15
- **题目**：给出两幅相同大小的 `0/1` 矩阵，统计相同位置的像素点数，输出相似度百分比，保留两位小数。
- **我的错误**：
  - 把 `100.0` 写成了 `100`；
  - 把分母 `(m * n)` 的括号漏掉/写错，例如写成 `100.0 * cnt / m * n`。
- **为什么错**：
  - 如果写成 `100 * cnt / (m * n)`，`100 * cnt` 和 `m * n` 都是整数，除法会先按整数除法计算，小数部分被丢掉。例如 `100 * 1 / 3` 得到 `33`，而不是 `33.333...`。
  - 如果写成 `100.0 * cnt / m * n`，`/` 和 `*` 优先级相同，会从左到右计算，实际变成 `((100.0 * cnt) / m) * n`，`n` 会跑到分子上，结果错误。
- **正确写法**：

```cpp
int cnt = 0;

for (int i = 0; i < m; i++) {
    for (int j = 0; j < n; j++) {
        if (a[i][j] == b[i][j]) {
            cnt++;
        }
    }
}

double similarity = 100.0 * cnt / (m * n);
cout << fixed << setprecision(2) << similarity << '\n';
```

- **输出格式提醒**：
  - 题目样例输出是 `44.44`，不是 `44.44%`，不要多打印百分号。
  - `cout` 写法需要 `fixed` 和 `setprecision(2)`。
  - 如果用 `printf`，用 `printf("%.2f\n", similarity);`；`%f` 是更标准的 `double` 写法。
- **记忆口诀**：

```text
百分比先转浮点：100.0
分母有多项就加括号：(m * n)
除法乘法同级，从左到右算，别把 n 漏到分子上
```

- **达标勾选**：□ 重新默写一次　□ 自造 `n=3` 的非整除数据测试　□ 检查输出是否保留两位小数且没有多余的 `%`

---

## 错题追记｜B2106 矩阵转置：转置后 m/n 搞反

- **日期**：2026-09-15
- **题目**：输入 `m` 行 `n` 列的矩阵，输出它的转置矩阵。
- **我的错误**：矩阵已经完成转置，但输出时仍然按原来的 `m` 行 `n` 列理解，导致 `m` 和 `n` 搞反。
- **为什么错**：
  - `m` 和 `n` 表示的是**原矩阵**的行数和列数。
  - 矩阵转置后，行列身份也互换：原来的列变成新行，原来的行变成新列。
  - 因此不能再机械地写“外层 `m`、内层 `n`”。
- **正确关系**：

```text
原矩阵：m 行 n 列
转置后：n 行 m 列

转置后输出：
外层循环 i < n    // 输出 n 行
内层循环 j < m    // 每行输出 m 个
```

- **正确输出骨架**：

```cpp
for (int i = 0; i < n; i++) {
    for (int j = 0; j < m; j++) {
        if (j) cout << ' ';
        cout << b[i][j];
    }
    cout << '\n';
}
```

- **如果直接从原矩阵输出转置结果**：

```cpp
for (int col = 0; col < n; col++) {
    for (int row = 0; row < m; row++) {
        if (row) cout << ' ';
        cout << a[row][col];
    }
    cout << '\n';
}
```

- **检查清单**：
  - 现在要输出几行？转置后是 `n` 行。
  - 每一行有几个数？转置后每行是 `m` 个。
  - 直接输出时，元素是不是 `a[row][col]`？
  - 方阵原地交换时，`j` 是不是从 `i + 1` 开始？
- **记忆口诀**：

```text
原 m×n，转置 n×m
原来的列变成新的行；
原来的行变成新的列。
外层看新行数，内层看新列数。
```

- **达标勾选**：□ 重新默写一次　□ 用 `2 × 3` 非方阵测试　□ 用 `3 × 3` 方阵测试　□ 检查行尾没有多余空格

---

## 使用备忘

- 达标顺序建议：**T21–T30 → T01–T03 → T04–T07 → T08–T09 → T10–T13 → T14–T17 → T18–T20**，跟着《蓝桥杯知识点备考清单》L0/L1 的进度走。
- 每个模板练熟后，在对应代码骨架的空白处把**自己最终版的完整代码**誊写一遍（作为个人参考版），并把你犯过的错补进"易错点"。
- 考前一周：把"易错点"全部扫一遍 + 每个模板快速默写一次（不求全对，找回手感）。

---

## 变式练习｜B2087 计数类（与指定数字相同的数的个数）

> 原题：洛谷 B2087。给 n 个数，再给一个 m，问 m 出现了几次。
> 练法：原题做熟后，用下面 4 个版本逐个重写，每个版本都**关掉参考默写**一遍。

### 版本 1：STL 一行计数（练 `count`）
- 关键：`cout << count(a.begin(), a.end(), m) << '\n';`
- 复杂度：O(n)
- 达标：□

### 版本 2：unordered_map 预统计（练哈希计数）
- 关键：`cnt[x]++` 统计，`cnt[m]` 查询（不存在时自动是 0）
- 复杂度：O(n) 预处理，之后每次查询 O(1)
- 达标：□

### 版本 3：排序 + 二分计数（练 lower_bound / upper_bound）
- 关键：`sort` 之后，`upper_bound(a.begin(), a.end(), m) - lower_bound(a.begin(), a.end(), m)`
- 复杂度：O(n log n)
- 达标：□

### 版本 4（重点变式）：多组查询
- 题目改法：第三行给 q，接下来 q 行每行一个 m，分别问 m 出现了几次
- 为什么重要：**每次重新遍历是 O(n·q)，会超时**，必须"预处理 + O(1) 查询"
- 核心代码：

```cpp
unordered_map<int,int> cnt;
for (int i = 0; i < n; i++) { int x; cin >> x; cnt[x]++; }

int q; cin >> q;
while (q--) {
    int m; cin >> m;
    cout << cnt[m] << '\n';       // 每次查询 O(1)
}
```

- 达标：□

> 口诀：**查 1 次 → 遍历就够；查很多次 → 先统计再查。**
